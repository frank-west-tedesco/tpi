const dontwannabeanamericanidiot = [i,was,meant,to,be,yours];
tiene metodos varios nativos, son 4 o mas 
1° defino variable
2° = asigno [variables] elementos
console.log ([3])
let miarray = new array();
length
let numeros = [10,20,30,40];
console.log (numeros.length);

/*estructura modificable similar a una lista o coleccion de datos
una variable que almacena varios datos/variables 
ordenan en posiciones 
invocamos estos datos en base a los indices numericos*/

arreglo padre{
    arreglo hijo1
    arreglo hijo2
    arreglo hijo3
}